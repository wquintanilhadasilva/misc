import { WspanDirective } from './wspan.directive';

describe('WspanDirective', () => {
  it('should create an instance', () => {
    const directive = new WspanDirective();
    expect(directive).toBeTruthy();
  });
});
